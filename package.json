{
  "name": "vexora-chat-app",
  "version": "1.0.0",
  "description": "A realtime chatting application with multiple user",
  "license": "ISC",
  "author": "fatinFuyad",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  }
}
